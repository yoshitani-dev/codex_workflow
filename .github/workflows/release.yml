name: Build and publish release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Semantic release tag, for example v1.1.0"
        required: true
        type: string
      prerelease:
        description: "Publish as a GitHub prerelease"
        required: true
        default: true
        type: boolean

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the tagged source
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref }}

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.11"

      - name: Install development checks
        run: python -m pip install -r requirements-dev.txt

      - name: Resolve and package the release
        id: package
        env:
          MANUAL_TAG: ${{ inputs.tag }}
        shell: bash
        run: |
          if [[ "$GITHUB_EVENT_NAME" == "push" ]]; then
            release_tag="$GITHUB_REF_NAME"
          else
            release_tag="$MANUAL_TAG"
          fi
          python -m ruff check codex_workflow scripts
          python -m mypy
          python -B scripts/test_workflow_runtime.py -v
          python -B scripts/test_orchestration_runtime.py -v
          python -B scripts/test_package_release.py -v
          python -B codex_workflow/workflow.py validate \
            --package-root codex_workflow \
            --json
          python scripts/package_release.py \
            --release-tag "$release_tag" \
            --output-dir dist
          python scripts/package_release.py \
            --verify dist/codex_workflow-*.zip
          source_sha="$(git rev-parse HEAD)"
          echo "tag=$release_tag" >> "$GITHUB_OUTPUT"
          echo "sha=$source_sha" >> "$GITHUB_OUTPUT"

      - name: Publish the GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ steps.package.outputs.tag }}
          RELEASE_SHA: ${{ steps.package.outputs.sha }}
          MANUAL_PRERELEASE: ${{ inputs.prerelease }}
        shell: bash
        run: |
          release_options=(--generate-notes)
          if [[ "$GITHUB_EVENT_NAME" == "push" || "$MANUAL_PRERELEASE" != "false" ]]; then
            release_options+=(--prerelease)
          fi
          gh release create "$RELEASE_TAG" \
            dist/codex_workflow-*.zip \
            dist/SHA256SUMS \
            --title "codex_workflow $RELEASE_TAG" \
            --target "$RELEASE_SHA" \
            "${release_options[@]}"
